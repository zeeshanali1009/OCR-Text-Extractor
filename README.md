# OCR‑Text‑Extractor

A Python‑based OCR image text extraction tool that uses Optical Character Recognition (OCR) to extract text from images and documents.

## 🔍 Overview

This project enables users to upload images and extract readable text using OCR technology. It can be used for scanned documents, photos with text, screenshots, and more.

OCR (Optical Character Recognition) allows converting images with text into editable, copyable text data using tools like Tesseract. :contentReference[oaicite:1]{index=1}

## 🚀 Features

- Extract text from various image formats (PNG, JPG, JPEG, GIF, BMP, etc.)  
- Preprocess image for better text clarity  
- Option to save extracted text to file  
- Easy‑to‑use script or CLI tool

## 📦 Requirements

Install dependencies:

```bash
pip install -r requirements.txt
